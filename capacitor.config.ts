import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Johannische-Kirche Gesangbuch',
  webDir: 'dist'
};

export default config;
